# Server
server.port=8080

# MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/catcafe?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Bangkok
spring.datasource.username=root
spring.datasource.password=Conan17970
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# ใน Hibernate 6 จะ auto-detect dialect ได้; ถ้าจะระบุ ให้ใช้ตัวนี้ก็ได้
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Static resources (ไม่ต้องตั้งก็ทำงานได้ ถ้าไฟล์อยู่ใน src/main/resources/static/)
# spring.web.resources.add-mappings=true   <-- เอาออกได้
